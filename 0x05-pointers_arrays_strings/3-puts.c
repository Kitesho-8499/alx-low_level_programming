#include "main.h"
/**
 * _puts - Print a string followed by a new line to stdoutf
 * @str: string to print
 */
void _puts(char *)
{
	while (*str != '\0')
	{
		_puts(char  str);
		str++;
	}
	_puts('\n');
}
