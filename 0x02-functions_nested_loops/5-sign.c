#include "main.h"
/**
 * print_sing - fuction that prints signs
 *
 * @n: fuction parameter
 *
 * Return: 1, 0 and -1
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (n == 0)
}
