#include "main.h"
/**
 * _isdigit - check for a digit (0 through 9)
 * @C: number
 * Return: 0 (if failed, 1 (success)
 */
int _isdigit(int c)
{
	int i;

	for (i= '0'; l <= '9'; i++)
	{
		if (i ==c)
		{
			return (i);
		}
	}
	return (0);
}
